/**
 * @desc A namespacessssssssss.
 * @namespace LinkedList
 * @access public
 * @alias MyNewNamespace
 *
 * @author Yair
 * @copyright 2016
 */
const MyObject = {
  /**
   * A function in MyNamespace (MyNamespace.myFunction).
   * @function myFunction
   * @memberof LinkedList
   *
   * @example
   * <caption>JSDoc3 Captions</caption>
   * var a = 0;
   *
   * for (var i = 0; i < 10; i++) {
   *     a++;
   * }
   *
   * @example
   * jaguar-jsdoc uses markdown style.
   *
   * ```
   * var a = 0;
   *
   * for (var i = 0; i < 10; i++) {
   *     a++;
   * }
   * ```
   */
  sayHello () {
    return 'Hello';
  }
}
